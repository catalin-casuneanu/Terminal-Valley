import utils from '../utils';

export default {
  erio: {
    // src: '/images/characters/people/erio.png',
    stats: {
      name: 'Erio',
      id: 's001',
      hp: utils.randomFromMinMax(60, 90),
      maxHp: 100,
      strength: utils.randomFromMinMax(60, 90),
      defense: utils.randomFromMinMax(40, 60),
      speed: utils.randomFromMinMax(40, 60),
      luck: utils.randomFromMinMax(20, 40),
      level: 1
    }
  },
  beth: {
    // src: '/images/characters/people/npc1.png',
    stats: {
      name: 'Beth',
      id: 's001',
      hp: utils.randomFromMinMax(60, 90),
      maxHp: 100,
      strength: utils.randomFromMinMax(60, 90),
      defense: utils.randomFromMinMax(40, 60),
      speed: utils.randomFromMinMax(40, 60),
      luck: utils.randomFromMinMax(20, 40),
      level: 1
    }
  },
  chefRootie: {
    // src: '/images/characters/people/secondBoss.png',
    stats: {
      name: 'Rootie',
      id: 'v002',
      hp: utils.randomFromMinMax(60, 90),
      maxHp: 100,
      strength: utils.randomFromMinMax(60, 90),
      defense: utils.randomFromMinMax(40, 60),
      speed: utils.randomFromMinMax(40, 60),
      luck: utils.randomFromMinMax(20, 40),
      level: 2
    }
  },
  streetNorthBattle: {
    // src: '/images/characters/people/npc8.png',
    stats: {
      name: 'Angry Chef',
      id: 's001',
      hp: utils.randomFromMinMax(60, 90),
      maxHp: 100,
      strength: utils.randomFromMinMax(60, 90),
      defense: utils.randomFromMinMax(40, 60),
      speed: utils.randomFromMinMax(40, 60),
      luck: utils.randomFromMinMax(20, 40),
      level: 1
    }
  },
  diningRoomBattle: {
    // src: '/images/characters/people/npc8.png',
    stats: {
      name: 'Fullstack Thug',
      id: 's001',
      hp: utils.randomFromMinMax(60, 90),
      maxHp: 100,
      strength: utils.randomFromMinMax(60, 90),
      defense: utils.randomFromMinMax(40, 60),
      speed: utils.randomFromMinMax(40, 60),
      luck: utils.randomFromMinMax(20, 40),
      level: 1
    }
  },
  streetBattle: {
    // src: '/images/characters/people/npc8.png',
    stats: {
      name: 'Werewolf',
      id: 'v002',
      hp: utils.randomFromMinMax(60, 90),
      maxHp: 100,
      strength: utils.randomFromMinMax(60, 90),
      defense: utils.randomFromMinMax(40, 60),
      speed: utils.randomFromMinMax(40, 60),
      luck: utils.randomFromMinMax(20, 40),
      level: 1
    }
  }
};
